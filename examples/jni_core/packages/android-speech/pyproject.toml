[build-system]
requires = ["setuptools>=68", "wheel", "cython>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "android-android-speech"
version = "35.0.0"
description = "Cython JNI wrappers for the android.speech.* subpackage (android.jar, API 35). Imports as android.android.speech.*."
requires-python = ">=3.11"
dependencies = [
    "jni_core",
    "android-android == 35.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.setuptools.package-data]
"*" = ["*.pxd", "*.pyi"]

[tool.setuptools.exclude-package-data]
"*" = [".DS_Store", "*.DS_Store", "*.c", "*.pyx", "*.pxi"]

[tool.cibuildwheel]
build = "cp313-android_*"

[tool.cibuildwheel.android]
archs = ["arm64_v8a", "x86_64"]
